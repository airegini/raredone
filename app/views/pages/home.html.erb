<div class="jumbotron">
	<center><h1>RareDone.</h1></center>

	<% if user_signed_in? %>

	Youre in!

	<% else %>

		<center> <%= link_to "Sign Up", new_user_registration_path, class: "btn btn-primary" %>
		  <%= link_to "Sign In", new_user_session_path, class: "btn btn-primary" %>
		</center>
	
	<% end %>

</div>